---
- name: deploy_monitoring_config
  hosts: all
  become: yes
  gather_facts: yes

  tasks:
    - name: update-system
      ansible.builtin.include_role:
        name: update-system

    - name: elk_server
      ansible.builtin.include_role:
        name: update-system

    - name: beats_agents
      ansible.builtin.include_role:
        name: update-system
